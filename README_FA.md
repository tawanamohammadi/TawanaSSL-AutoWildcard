# TawanaSSL-AutoWildcard (نسخه فارسی)

<p align="center">
  <img src="https://github.com/user-attachments/assets/60a2a106-3c0e-49ec-8ac7-7ce0348ce765" width="85%" />
</p>

<p align="center">
  <img src="https://img.shields.io/badge/license-MIT-green">
  <img src="https://img.shields.io/badge/language-Bash-blue">
  <img src="https://img.shields.io/badge/DNS-Cloudflare-orange">
</p>

---

### **نصب‌کننده خودکار گواهینامه SSL برای مرزبان، مرزنشین، پاسارگاد، X-UI و سرورهای لینوکس عمومی**  
استفاده از **Cloudflare DNS + acme.sh + Let’s Encrypt**

🔗 **مخزن گیت‌هاب:** https://github.com/tawanamohammadi/TawanaSSL-AutoWildcard  
🌐 **وب‌سایت نویسنده:** https://tawana.online

TawanaSSL-AutoWildcard یک اسکریپت بش (Bash) تعاملی و ساده است که پروسه صدور و نصب گواهینامه‌های **Wildcard Let’s Encrypt** را با استفاده از **Cloudflare DNS** خودکار می‌کند. این ابزار به ویژه برای پنل‌های **مرزبان**، **مرزنشین**، **پاسارگاد**، **X-UI** و هر نوع وب‌سرور شخصی‌سازی شده طراحی شده است.

این ابزار تمامی مراحل را برای شما انجام می‌دهد:

* نصب **acme.sh** (در صورت عدم وجود)
* صدور گواهینامه Wildcard برای `domain` و `*.domain`
* استفاده از روش **Cloudflare DNS-01 challenge** (بدون نیاز به پورت ۸۰)
* نصب گواهینامه‌ها در مسیرهای اختصاصی مرزبان / مرزنشین / پاسارگاد / X-UI
* بروزرسانی خودکار فایل‌های تنظیمات (مانند `.env` در مرزبان و پاسارگاد)
* تهیه نسخه پشتیبان از گواهینامه‌های قدیمی به صورت ایمن
* بارگذاری مجدد Nginx و ری‌استارت سرویس‌های پنل
* تمدید خودکار گواهینامه‌ها از طریق Cron Job

---

## 🚀 ویژگی‌ها

* ✅ **پشتیبانی از Wildcard SSL**
  * صدور گواهینامه همزمان برای دامنه اصلی و تمامی زیردامنه‌ها:
    * `yourdomain.com`
    * `*.yourdomain.com`

* ✅ **یکپارچگی با DNS کلودفلر**
  * استفاده از پروتکل DNS کلودفلر با امنیت بالا.

* ✅ **نصب و تمدید کاملاً خودکار**
  * تنظیم Let’s Encrypt به عنوان مرجع صدور پیش‌فرض.
  * اطمینان از نصب مجدد گواهینامه در مسیر صحیح پس از تمدید خودکار.

* ✅ **سازگار با پنل‌های محبوب**
  * پشتیبانی پیش‌فرض از مسیرهای:
    * `/var/lib/marzban/certs`
    * `/var/lib/marzneshin/certs`
    * `/var/lib/pasarguard/certs`
    * `/etc/x-ui/certs`
  * امکان انتخاب **مسیر سفارشی** برای سایر سرویس‌ها.

* ✅ **مدیریت سرویس‌ها**
  * ری‌استارت خودکار پنل‌ها پس از نصب گواهینامه برای اعمال تغییرات.

* ✅ **امنیت و پشتیبان‌گیری**
  * ایجاد نسخه پشتیبان با برچسب زمان (Timestamp) برای جلوگیری از دست رفتن فایل‌های قبلی.

---

## 🧩 موارد استفاده

* امن‌سازی پنل مرزبان و زیرساخت‌های پروکسی.
* امن‌سازی استقرار مرزنشین.
* امن‌سازی پنل پاسارگاد و X-UI.
* صدور HTTPS برای تعداد نامحدود زیردامنه.

اگر از هر نوع پنل مدیریت پروکسی یا سرور شخصی استفاده می‌کنید، این اسکریپت دردسرهای SSL را برای شما حذف می‌کند.

---

## 📋 نیازمندی‌ها

۱. **سرور لینوکس با دسترسی root** (ترجیحاً اوبونتو ۲۰ یا بالاتر).
۲. **دامنه‌ای که در کلودفلر مدیریت می‌شود**.
۳. **اطلاعات کلودفلر**:
    * ایمیل اکانت کلودفلر.
    * Global API Key (از بخش پروفایل کلودفلر قابل دریافت است).

> 🔐 کلید API شما حساس است. این اسکریپت آن را ذخیره نمی‌کند و فقط در زمان اجرا از آن استفاده می‌کند.

---

## 🌐 تنظیمات DNS و جادوی Wildcard

قبل از اجرای اسکریپت، این موارد را در **پنل کلودفلر** خود رعایت کنید:

۱. **ثبت A Record**: یک رکورد از نوع A برای دامنه اصلی یا ساب‌دامین خود بسازید که به **آی‌پی سرور** شما اشاره کند.
۲. **پوشش همگانی (Wildcard)**: پس از اینکه اسکریپت گواهینامه را برای `*.yourdomain.com` صادر کرد، **تمامی ساب‌دامین‌های شما** به صورت خودکار امن می‌شوند.
    *   `panel.yourdomain.com` ✅ امن شده
    *   `node1.yourdomain.com` ✅ امن شده
    *   `هرچیزی.yourdomain.com` ✅ امن شده

شما **نیازی ندارید** برای هر ساب‌دامین جداگانه گواهینامه بگیرید. یک گواهینامه Wildcard برای همه کافیست!

---

## 📦 نصب و اجرا

مخزن را کلون کرده و دسترسی اجرایی به فایل بدهید:

```bash
git clone https://github.com/tawanamohammadi/TawanaSSL-AutoWildcard.git
cd TawanaSSL-AutoWildcard

chmod +x setup_ssl.sh
sudo ./setup_ssl.sh
```

---

## 🛠 مراحل اجرا (Step-by-Step)

پس از اجرای اسکریپت، ۶ مرحله زیر را طی می‌کنید:

### ۱. اطلاعات کلودفلر
ایمیل و API Key خود را وارد کنید (فیلد کلید مخفی است).

### ۲. تنظیم دامنه
دامنه اصلی خود را وارد کنید (مثال: `tawana.online`).

### ۳. انتخاب مسیر و پنل
پنل خود را انتخاب کنید. اگر از مرزبان یا پاسارگاد استفاده کنید، اسکریپت فایل `.env` را نیز برای شما بروزرسانی می‌کند.

### ۴. نصب acme.sh
اگر این ابزار روی سیستم نباشد، اسکریپت آن را نصب و تنظیم می‌کند.

### ۵. صدور گواهینامه
اسکریپت با کلودفلر ارتباط برقرار کرده و گواهینامه Wildcard را صادر می‌کند.

### ۶. نصب و پایان
گواهینامه‌ها در مسیر انتخاب شده کپی شده، تنظیمات اعمال می‌گردد و سرویس‌ها ری‌استارت می‌شوند. در انتها یک خلاصه به شما نمایش داده می‌شود.

---

## 🔁 تمدید خودکار

ابزار **acme.sh** به صورت خودکار یک Cron Job روی سیستم شما نصب می‌کند. گواهینامه‌ها حدوداً هر ۶۰ روز یکبار به صورت خودکار تمدید می‌شوند و این اسکریپت تضمین می‌کند که پس از هر تمدید، فایل‌ها به مسیر صحیح کپی شده و سرویس‌ها ری‌استارت گردند.

---

## 👤 نویسنده

**توانا محمدی**
محقق هوش مصنوعی و زیرساخت‌های امنیتی
🌐 وب‌سایت: [https://tawana.online](https://tawana.online)

اگر این ابزار برایتان مفید بود، لطفاً در گیت‌هاب به آن ⭐ بدهید.

---

## 📜 لایسنس

این پروژه تحت لایسنس **MIT** منتشر شده است. استفاده و تغییر در آن آزاد است.

---
🔐 با آرزوی اینترنتی امن برای همه.
